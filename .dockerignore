# The build context is the repository root (see service/Dockerfile), so this keeps the pipeline's
# test media, the iOS app and every local virtualenv out of the image.
.git
.github
.venv
**/.venv
**/__pycache__
**/*.pyc
.pytest_cache
.ruff_cache
ios
tests
dev_plan
retro
docs
