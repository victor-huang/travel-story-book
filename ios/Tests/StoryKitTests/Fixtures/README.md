# StoryKitTests fixtures

Committed artifacts bundled into the test target. Media fixtures stay in
`tests/fixtures/media/` (resolved from the repo via `#filePath`); this directory holds only
artifacts that exist for the Swift side, such as the committed `trip.json` decoded by
`TripDocumentTests`.
